name: Cross-compile QEMU Static for Android

on: workflow_dispatch

jobs:
  build:
    runs-on: ubuntu-24.04-arm
    steps:
      - run: |
          sudo apt-get update
          sudo apt-get install -y \
            git build-essential python3 ninja-build pkg-config \
            libglib2.0-dev libpixman-1-dev libslirp-dev libusb-1.0-0-dev \
            libgcrypt20-dev libfdt-dev libcap-ng-dev zlib1g-dev

          git clone --depth 1 --branch v11.0.0 https://git.qemu.org/git/qemu.git
          cd qemu
          mkdir build && cd build

          ../configure \
            --target-list=aarch64-softmmu \
            --enable-kvm \
            --disable-werror \
            --disable-guest-agent \
            --static \
            --disable-docs \
            --extra-cflags="-DCONFIG_ARM_VIRT_16K_PAGE=1 -static -O2" \
            --extra-ldflags="-static"

          make -j$(nproc)
          strip qemu-system-aarch64

      - uses: actions/upload-artifact@v4
        with:
          name: qemu-static-android
          path: qemu/build/qemu-system-aarch64
